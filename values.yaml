domain: lol-analysis.apps.drgdevlab.com

image:
  pullPolicy: IfNotPresent
  # GHCR images are public by default. Set this only if package visibility is private.
  pullSecret: ""
  backend:
    repository: ghcr.io/devsecopslonghn/lol-analysis-backend
    tag: "708c33be224ad9ff837ae83c43399d3c45d0b849"
  frontend:
    repository: ghcr.io/devsecopslonghn/lol-analysis-frontend
    tag: "9e039c2d9da95407a3a4cfc6d6f02cbb91c85b99"

backend:
  replicas: 1
  port: 8080
  resources:
    requests: {cpu: 100m, memory: 256Mi}
    limits: {cpu: "1", memory: 1Gi}

frontend:
  replicas: 1
  port: 8080
  resources:
    requests: {cpu: 50m, memory: 64Mi}
    limits: {cpu: 500m, memory: 256Mi}

persistence:
  enabled: true
  storageClassName: longhorn-workers
  accessModes: [ReadWriteOnce]
  size: 10Gi

ingress:
  # Keep the report API private until an auth proxy is configured.
  enabled: false
  className: nginx

networkPolicy:
  enabled: false

podAnnotations: {}
